{
  "name": "cv-oukkal-yacine",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "devDependencies": {
    "vitepress": "^2.0.0-alpha.12",
    "gh-pages": "^5.0.0"
  },
  "scripts": {
    "docs:dev": "vitepress dev",
    "docs:build": "vitepress build",
    "docs:preview": "vitepress preview",
    "deploy": "npm run docs:build && npx gh-pages -d .vitepress/dist"
  }
}